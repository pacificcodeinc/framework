import React from "react";
import ReactDOM from "react-dom/client";
import { App } from "@framework/app";
import "@framework/ui/styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
